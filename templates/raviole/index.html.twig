{% extends 'base.html.twig' %}

{% block title %}Raviole index{% endblock %}

{% block body %}
    <h1>Raviole index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for raviole in ravioles %}
            <tr>
                <td>{{ raviole.id }}</td>
                <td>{{ raviole.titre }}</td>
                <td>{{ raviole.description }}</td>
                <td>{{ raviole.prix }}</td>
                <td>{{ raviole.quantite }}</td>
                <td>
                    <a href="{{ path('raviole_show', {'id': raviole.id}) }}">show</a>
                    <a href="{{ path('raviole_edit', {'id': raviole.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('raviole_new') }}">Create new</a>
{% endblock %}
