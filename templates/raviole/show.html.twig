{% extends 'base.html.twig' %}

{% block title %}Raviole{% endblock %}

{% block body %}
    <h1>Raviole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ raviole.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ raviole.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ raviole.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ raviole.prix }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ raviole.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('raviole_index') }}">back to list</a>

    <a href="{{ path('raviole_edit', {'id': raviole.id}) }}">edit</a>

    {{ include('raviole/_delete_form.html.twig') }}
{% endblock %}
